namespace Crm.Cli;

/// <summary>Console host. Runs with zero command-line arguments; see handout §10.</summary>
public static class Program
{
    public static int Main()
    {
        Console.WriteLine("CRM Workflow Extractor — no stages implemented yet.");
        return 0;
    }
}
